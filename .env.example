# Copia este archivo a .env y completa los valores

# Admin
ADMIN_EMAILS=benja.huerta.mallea@gmail.com

# JWT (opcional)
SECRET_KEY=cambia_esta_clave_por_una_larga_aleatoria
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend base (para construir links del email)
FRONTEND_BASE_URL=http://127.0.0.1:8004/app

# SMTP (elige un proveedor y completa)
# Gmail (requiere App Password y 2FA habilitado):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_app_password
SMTP_FROM=tu_correo@gmail.com

# Mailtrap (sandbox)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=tu_usuario_mailtrap
# SMTP_PASS=tu_password_mailtrap
# SMTP_FROM=no-reply@naturalpower.cl
